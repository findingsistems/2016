export const DATE_FORMAT = 'DD.MM.YYYY'

export const NUMBER_FORMAT = {
  style: 'decimal',
}

export const CURRENCY_FORMAT = {
  style: 'decimal',
  minimumFractionDigits: 2,
  maximumFractionDigits: 2,
}

export const CURRENCY_WITH_NAME_FORMAT = {
  style: 'currency',
}

export const CURRENCY_WITH_CODE_FORMAT = {
  style: 'currency',
  currencyDisplay: 'code',
}

export const CURRENCY_RATE_FORMAT = {
  style: 'decimal',
  minimumFractionDigits: 4,
  maximumFractionDigits: 4,
}

export const PERCENT_FORMAT = {
  style: 'percent',
}

export const PERCENT_WITH_FRACTION_FORMAT = {
  style: 'percent',
  minimumFractionDigits: 2,
  maximumFractionDigits: 2,
}
